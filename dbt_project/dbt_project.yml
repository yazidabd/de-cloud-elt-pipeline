name: 'elt_cloud_pipeline'
version: '1.0.0'
config-version: 2
profile: 'elt_cloud_pipeline'

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  elt_cloud_pipeline:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
